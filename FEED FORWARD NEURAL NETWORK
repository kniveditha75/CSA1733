import numpy as np

def sigmoid(x):
    return 1 / (1 + np.exp(-x))

def feed_forward_nn(inputs, weights):
    return sigmoid(np.dot(inputs, weights))

# Example Usage
inputs = np.array([0.1, 0.2, 0.7])
weights = np.array([0.3, 0.5, 0.8])

output = feed_forward_nn(inputs, weights)
print(output)
